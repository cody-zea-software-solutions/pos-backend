/*import { Test, TestingModule } from '@nestjs/testing';
import { ServiceTransactionsController } from './service-transactions.controller';
import { ServiceTransactionsService } from './service-transactions.service';

describe('ServiceTransactionsController', () => {
  let controller: ServiceTransactionsController;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [ServiceTransactionsController],
      providers: [
        {
          provide: ServiceTransactionsService,
          useValue: {},
        },
      ],
    }).compile();

    controller = module.get<ServiceTransactionsController>(ServiceTransactionsController);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});*/
