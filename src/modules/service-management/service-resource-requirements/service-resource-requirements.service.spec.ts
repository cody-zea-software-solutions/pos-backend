/*import { Test, TestingModule } from '@nestjs/testing';
//import { ServiceResourceRequirementsService } from './service-resource-requirements.service';

describe('ServiceResourceRequirementsService', () => {
 // let service: ServiceResourceRequirementsService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [ServiceResourceRequirementsService],
    }).compile();

    service = module.get<ServiceResourceRequirementsService>(ServiceResourceRequirementsService);
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});*/
