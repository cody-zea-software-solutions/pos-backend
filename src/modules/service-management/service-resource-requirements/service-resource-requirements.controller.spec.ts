/*import { Test, TestingModule } from '@nestjs/testing';
import { ServiceResourceRequirementsController } from './service-resource-requirements.controller';

describe('ServiceResourceRequirementsController', () => {
  let controller: ServiceResourceRequirementsController;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [ServiceResourceRequirementsController],
    }).compile();

    controller = module.get<ServiceResourceRequirementsController>(ServiceResourceRequirementsController);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
*/